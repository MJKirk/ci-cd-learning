name: Bash testing
on: push
jobs:
  job_1:
    name: Simple bash test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Check the script runs
        run: bash/simple_script.sh
        
